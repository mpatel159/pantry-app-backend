{
    "$schema": "http://json-schema.org/draft/2020-12/schema",
     "title": "Image",
     "description": "A simple Image prcessing format",
     "type": "object",
     "properties": {
        "itemName": {
            "description": "The name of the item in the image",
            "type": "string"
        },
        "quantity": {
            "description": "the quantity of the item in the image",
            "type": "number"
        }
     }
}